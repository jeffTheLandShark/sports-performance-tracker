Steps to run:

1. Install dependencies: `npm install`
2. Start dev backend: `npm run dev`

This will start the backend on http://localhost:3000. The backend contains all of the database queries in API routes under `src/routes`.