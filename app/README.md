Steps to run:

1. Install dependencies: `pnpm install` or `npm install`
2. Start frontend: `pnpm exec vite`

This will start the frontend on http://localhost:5173. The frontend will automatically connect to the backend at http://localhost:3000